{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the optMusic desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
